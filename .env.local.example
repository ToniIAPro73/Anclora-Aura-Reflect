# Copy to .env.local and adjust as needed
GEMINI_API_KEY=your-gemini-api-key

# Local engine (FastAPI backend) URL
VITE_LOCAL_ENGINE_URL=http://localhost:8000

# Legacy backend URL (if you have another service)
VITE_BACKEND_URL=http://localhost:5050

# Optional cloud fallback for image generation (expects same API as local engine)
# Set to your Hugging Face Space URL to enable Cloud mode and Auto (fallback) in the UI.
VITE_CLOUD_ENGINE_URL=https://ToniBalles73-Anclora.hf.space
