# --- Frontend / API remota ---
# Clave de Gemini utilizada por el servicio frontend cuando se trabaja contra la nube.
GEMINI_API_KEY=

# URL opcional de un backend local. Si se deja vacío, el frontend usará la API de Gemini.
IMAGE_SERVICE_BASE_URL=http://localhost:4000

# URL opcional de un motor de nube para fallback. Debe exponer los endpoints /generate y /refine.
VITE_CLOUD_ENGINE_URL=

# --- Servicio de inferencia local ---
# Ruta absoluta o relativa al checkpoint base de Stable Diffusion XL.
MODEL_CHECKPOINT_DIR=./models/sdxl-base

# Ruta al checkpoint del refiner (opcional pero recomendado para mayor calidad).
MODEL_REFINER_CHECKPOINT_DIR=./models/sdxl-refiner

# Dispositivo a utilizar por el runtime de difusión. Valores comunes: cuda, cuda:0, cpu, mps.
MODEL_DEVICE=cuda

# Precisión numérica para la inferencia. Valores válidos: fp32, fp16, bf16.
MODEL_PRECISION=fp16

# Activa optimizaciones de memoria como xFormers cuando está disponible (1=true, 0=false).
ENABLE_XFORMERS=1

# Controla el tamaño de los lotes enviados al modelo para equilibrar uso de memoria y latencia.
MODEL_MAX_BATCH_SIZE=2

# Puerto en el que el backend local expone sus endpoints REST.
BACKEND_PORT=4000
